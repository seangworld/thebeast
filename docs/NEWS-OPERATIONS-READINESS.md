# BeastAdmin News operational evidence

Part of the authorized September operating batch. The existing News Operations and Analytics surfaces now accept either boolean value of publicAutoPublishing. Previously, enabled publication caused the entire otherwise valid public status response to be rejected. All consumed nested fields are validated before rendering; missing coverage remains unavailable rather than fabricated zero.

Configuration and publication-authority indicators remain unchanged. A separate read-only Fact Desk observation labels healthy, degraded, running, stale or unknown worker evidence and shows its last completion time. A snapshot older than one hour, invalid/future timestamps, duplicate/missing workers or malformed evidence cannot establish health. Three missed 15-minute cadences (45 minutes) makes the last worker evidence stale. A later start supersedes an earlier completion. Historical persistent-account-limit evidence is described as the latest recorded failure, not confirmation of a current credit balance. Later healthy evidence clears that historical warning.

The assessor does not call providers, write to storage, restart workers, change watchdog behavior, authorize publication, relax QA gates, spend or alter credentials. It reads only the existing public News status endpoint. The broader provider availability issue requires fresh authorized evidence; funding or rebinding is not inferred from this work.

Product Completeness: existing owner-only surfaces, same navigation, existing text styles, no module version or schema change. Tests cover enabled/disabled publication, malformed consumed fields, stale/degraded/future/missing/duplicate evidence and recovery. Full tests, types, lint, build and independent review are required. Authenticated responsive visual verification remains unperformed under the existing owner batch deferral. Rollback is a code revert; no data rollback. Real generation recovery remains unproven until a fresh worker completion is observed.
