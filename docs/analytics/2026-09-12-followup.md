# Analytics follow-up — 12 September 2026

## Confirmed

- Production News traffic endpoint returned HTTP 200 with `status: ready` and `pageViews: 0` for the rolling 24-hour window ending 2026-09-12 19:51 UTC. This is a validated GA4 report response, not an unavailable-provider fallback.
- BeastAdmin GA4 daily summaries intentionally end on the previous UTC calendar date. A synchronization timestamp does not mean today's events are in those reports.
- This change exposes the requested report end date and explains that today is excluded and recent counts can change. GA4 synchronization freshness is labeled separately from data freshness. It does not change collection, report filters, or zero-count handling.
- Both sites' page-view builders preserve the browser origin in `page_location`; the News report filters the exact `news.seangworld.com` hostname.
- Current measurement IDs: SEANGWORLD/News `G-YFRV4QJK04`; Beast `G-QC08Q29MCC`. The numeric Data API property ID is a separate identifier and must be verified against the owning property in Google Analytics.

## Still unverified

Actual ingestion is not confirmed. There is no authenticated Google Analytics or Search Console session available in this workspace. The valid zero response does not establish why visits are missing.

Next account checks, in priority order:

1. Match both measurement IDs to the intended GA4 data streams and confirm the reporting service's numeric property contains those streams.
2. Open Realtime/DebugView while visiting each production hostname, then verify page_view delivery and any active internal/developer traffic filters.
3. Compare processed reports after collection is confirmed. Do not attribute all missing traffic to processing delay without evidence.
4. Verify Search Console ownership, submitted sitemaps and page indexing; complete Bing verification where applicable.

Google documents that processing may take 24–48 hours and report values can change: https://support.google.com/analytics/answer/11198161?hl=en

## Remaining editorial SEO

Story-specific imagery needs approved assets and per-image source, credit, licensing and alt text. The current FactBriefDraft model has no image provenance fields. No third-party article images have been copied or generated as purported news evidence. Original reporting, substantive local coverage and earned links remain ongoing editorial work.

The preceding technical SEO releases and production crawl evidence are recorded in `seangworld/seangworld.com/docs/SEO-REMEDIATION-2026-09-12.md`.

## Owner connection check

BeastAdmin → SEANGWORLD Intelligence now includes **Check analytics connection**, available independently of the daily report load. It uses the existing authenticated admin route and Google analytics.readonly connection to list streams in BEAST_ECOSYSTEM_GA4_PROPERTY_ID. It compares the current Beast environment measurement ID and the SEANGWORLD/News production baseline G-YFRV4QJK04 (reverified from the main site's live script on 12 September).

The result distinguishes matched property membership, missing expected streams, and an unavailable/incomplete check. Matching does not prove event ingestion. Permission failures may mean the Analytics Admin API is disabled or the reporting service cannot read the property; no permissions are automatically expanded. Paginated responses are bounded and fully processed before absence is reported. No new environment variables, database changes, public diagnostics, or tracking configuration changes are introduced.

Google reference: https://developers.google.com/analytics/devguides/config/admin/v1/rest/v1beta/properties.dataStreams/list

Acceptance: 2,071 tests passed, including owner-route denial before provider access, membership/pagination, malformed and foreign-property responses, provider failures and credential-safe errors. Full lint/type/build gates are required before release. The production Google result still requires running the check as the signed-in owner; do not close the ingestion incident on implementation evidence alone.
